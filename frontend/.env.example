# Copy to ".env" for local overrides (rarely needed — defaults already match
# `npm run dev` talking to the backend on localhost:4000). Docker Compose and
# real deployments set these at BUILD time (see root docker-compose.yml).

VITE_API_URL=http://localhost:4000/api/v1
VITE_SOCKET_URL=http://localhost:4000
